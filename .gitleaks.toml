# Gitleaks configuration (see https://github.com/gitleaks/gitleaks)
title = "NGO Databank"

[extend]
useDefault = true

[allowlist]
description = "Env templates and docs — placeholders only, no live credentials"
paths = [
    '''Backoffice/env.example''',
]
