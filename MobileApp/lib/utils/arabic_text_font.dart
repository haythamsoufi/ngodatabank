/// Arabic script typography for the app (when UI locale is Arabic).
enum ArabicTextFontPreference {
  /// Packaged Tajawal — matches IFRC / web styling.
  tajawal,

  /// Platform UI font (e.g. **SF Pro** on iOS, **Roboto** / **Noto** on Android).
  system,
}
